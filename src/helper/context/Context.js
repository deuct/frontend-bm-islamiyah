import { createContext } from "react";

export const DataContext = createContext({});

export const NBListingContext = createContext({});

export const TRListingContext = createContext({});

export const AuthContext = createContext({});
